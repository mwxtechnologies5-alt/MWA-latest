import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "X (Twitter) Trending Services",
  description:
    "Generate massive buzz and reach millions. Our X (Twitter) trending campaigns guarantee top-tier visibility for your project launch.",
  keywords:
    "X trending service, Twitter marketing, viral campaign, crypto Twitter promotion",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
